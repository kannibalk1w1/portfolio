import type { BlueprintsSection as BlueprintsSectionType } from '../../types/portfolio'

export function BlueprintsSection({ section: _section }: { section: BlueprintsSectionType }) {
  return <div style={{ color: '#aaa', fontSize: 13 }}>Blueprints section (coming soon)</div>
}
